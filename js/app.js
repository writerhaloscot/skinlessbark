$(function () {

    var sec = 1000;

    // CHECK FOR LOW POWER MODE
    /* const isSafari = /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
    document.querySelector('#skinlessbark video').play().then(() => {
        console.log('Low Power Mode Off');
    }).catch((error) => {
        if (error.name === 'NotAllowedError') {
            console.log('Low Power Mode On');
            if (isSafari) {
                alert('Please turn low power mode off and reload the browser.');
            }
        }
    }); */



});
